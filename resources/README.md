# resources/

Place your `.chm` source files here before running the converter.

```bash
python chm_to_markdown.py --single resources/docs.chm
# or convert everything in this folder
python chm_to_markdown.py --all
```

`.chm` files are gitignored and not committed to this repo — add your own copies locally.
